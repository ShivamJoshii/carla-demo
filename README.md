# Carla — AI Office Manager (demo)

Scripted product demo for the AI Office Manager. Press Enter to advance the
conversation; click Approve on the gate cards. Refresh to restart.

Static site — deploy anywhere (Netlify: publish directory = repo root).
